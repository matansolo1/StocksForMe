# Project Context: StocksForMe Stock Scanner

מסמך זה הוא מקור האמת היחיד (Single Source of Truth) לפרויקט - ארכיטקטורה, אסטרטגיית מסחר, כללי הפעלה שבועיים, וממצאי מחקר.

---

## 1. ארכיטקטורת הפרויקט

מערכת קלה (lightweight), מקומית, לסריקת מניות ומעקב פורטפוליו - בנויה עם Python Flask ו-Tailwind CSS. סורקת יקום של כ-100 טיקרים לאיתור הזדמנויות Swing Trading מבוססות אינדיקטורים טכניים (EMA, RSI, MACD, Volume, ATR) ומנהלת עסקאות פעילות.

### מבנה קבצים ותפקידים

* **`app.py`** - נקודת הכניסה של Flask. מגדיר את כל ה-routes:
    * `/` - הדשבורד הראשי.
    * `/run-scan`, `/execute-scan` - טופס ותהליך סריקה שבועית.
    * `/api/scan-stream` - Server-Sent Events (SSE) שמריץ את הסורק בזמן אמת, מוסיף עסקאות חדשות, ומרענן את הדשבורד.
    * `/api/dry-run-stream` - סריקת ניסיון (dry run) ללא שמירה במסד הנתונים.
    * `/api/backtest` - מריץ בקטסט week-by-week ומחזיר סטטיסטיקות + עקומת הון.
    * `/trade-analytics`, `/api/trade-analytics` - דשבורד אנליטיקס.
    * `/under-the-hood` - תיעוד חי של האסטרטגיה.
* **`scanner.py`** - מנוע הסריקה. `scan_universe_generator()` - generator שסורק את היקום, כולל פילטר מגמת שוק גלובלי (SPY מעל SMA 200). **אסטרטגיית הייצור היא Momentum בלבד.**
* **`backtester.py`** - מנוע בקטסט week-by-week על שתי תקופות שוק (Calm/Chaos), עם caching של נתונים היסטוריים (`cache/*.pkl`).
* **`monte_carlo_backtester.py`** - סימולציית Monte Carlo לקביעת סדר פגיעה ב-SL/TP בתוך יום מסחר בודד, לדיוק גבוה יותר בבקטסט.
* **`trading_logic.py`** - מנוע מחזור החיים של עסקה: הוספת הוראות לימיט חדשות (`add_new_trades`), **אימות מילוי ההוראה מול נרות 5 דקות** (`check_pending_entries` / `evaluate_limit_fill`), עדכון סטטוס פוזיציות (SL/TP), וקבועי/פרדיקטי הסטטוסים המשמשים כמקור אמת יחיד לכל המערכת (`is_open_position`, `holds_slot`, `is_closed_trade`).
* **`audit_entries.py`** - כלי CLI לביקורת רטרואקטיבית: מזהה פוזיציות `ACTIVE` שבפועל מעולם לא נקנו ("פוזיציות פנטום") ומאפשר לתקנן. dry-run כברירת מחדל, `--apply` לביצוע (עם גיבוי אוטומטי).
* **`tracker.py`** / **`data_manager.py`** - ניהול `trades_db.json`: עסקאות פעילות, היסטוריה, ומטא-דאטה של הפורטפוליו.
* **`currency_manager.py`** - ניהול `deposits_history.json`: הפקדות, המרת ILS→USD, ועמלות המרה, ושערי חליפין (חיים/היסטוריים/ברירת מחדל הניתנים לעריכה).
* **`ui_generator.py`** - יצירת דשבורד HTML עם Tailwind CSS וגרפי Chart.js/Plotly, כולל "Strategy Cheat Sheet & Robustness Matrix".
* **`analytics_generator.py`** - חישובי אנליטיקס וניהול מצב פורטפוליו:
    * `calculate_portfolio_state()` - שווי הון, מזומן זמין, רווח/הפסד ממומש/לא ממומש.
    * `calculate_position_size()` - גודל פוזיציה לפי מזומן זמין חלקי סלוטים פנויים.
    * `calculate_mwr_cumulative_return()` - תשואה מצטברת משוקללת-הון (מבוססת על ההפקדות האמיתיות בפועל).
    * `simulate_strategy_growth()` - **סימולציית "מה היה קורה אם"**: מדמה חשבון בודד של $10,000 שעוקב אחרי *כל* הסיגנלים שהאסטרטגיה אי-פעם ייצרה, מההתחלה, עם חלוקת מזומן זמין שווה בין עד 3 סלוטים פתוחים (חוק 1/3-1/3-1/3), כולל עמלות. עונה על השאלה "אם הייתי נכנס מהיום הראשון עם סכום קבוע ועוקב אחרי השיטה במדויק - כמה הייתי מרוויח?" - בניגוד ל-MWR שמשקף את הכסף האמיתי (עם הפקדות מדורגות, ריבוי owners, וגדלי פוזיציה לא-אחידים).
    * `_dedupe_signals()` - מאחדת עסקאות כפולות (אותה מניה, אותו entry/exit price, נרשמו פעמיים - אחת לכל owner) לסיגנל אחד, כדי שהסימולציה תמדוד החלטת-מסחר אחת ולא תכפיל אותה.
    * `prepare_analytics_data()` - מכין את כל הנתונים לדשבורד האנליטיקס.
* **`stock_api.py`** - ממשק ל-Yahoo Finance (מחירים חיים, נתונים היסטוריים, תוך-יומיים, תאריכי דוחות).
* **`finance_utils.py`** - פונקציות עזר: `calculate_rsi`, `calculate_pnl_pct`, `calculate_realized_usd`, `calculate_mwr`.

---

## 2. אסטרטגיית הייצור (Momentum Scenario 3 - המנצחת)

1. **פילטר מגמה גלובלי**: עסקאות מתבצעות רק כאשר מדד ה-S&P 500 שורי:
   $$\text{SPY Close} > \text{SPY SMA}_{200}$$

2. **טריגר כניסה**: RSI 14 חוצה מעל 55 כאשר המחיר מעל SMA 50:
   $$\text{RSI}_{\text{prev}} < 55 \quad \text{and} \quad \text{RSI}_{\text{current}} \ge 55 \quad \text{and} \quad \text{Price} > \text{SMA}_{50}$$

3. **ניהול סיכונים**: Stop Loss קבוע של 5.0%: $\text{SL} = \text{Price} \times 0.95$

4. **יעד רווח**: Take Profit קבוע של 10.0% (יחס סיכון/סיכוי 1:2): $\text{TP} = \text{Price} \times 1.10$

**ביצועים:** +100.57% בתקופת הכאוס (2021-2026), +232.78% בתקופה הרגועה (2010-2020), Win Rate יציב של ~43% בשני המשטרים. **+567.8%** תשואה מצטברת על פני 16 שנה.

**הערה:** אסטרטגיית Mean Reversion (RSI<30) הוסרה לחלוטין מהקוד והייצור עקב ביצועים גרועים בשווקים תנודתיים (-30.93%).

### טבלת מבחני חוסן (Robustness Matrix)

| Strategy | RSI | SL | TP | Chaos Era | Calm Era | Full (16y) | Max DD |
|---|---|---|---|---|---|---|---|
| Mean Reversion | 30 | 3.0% | 6.0% | -30.93% (WR 33.2%) | +63.34% (WR 35.5%) | +12.8% | -38.8%/-44.9% |
| Momentum (Default) | 60 | 5.0% | 12.0% | -4.88% (WR 31.2%) | +148.73% (WR 37.5%) | +136.6% | -33.5%/-20.6% |
| Momentum (Scenario 1) | 65 | 4.0% | 12.0% | +12.94% (WR 27.6%) | +198.16% (WR 34.5%) | +236.7% | -27.3%/-19.5% |
| Momentum (Scenario 2) | 60 | 7.5% | 15.0% | -8.41% (WR 34.0%) | +268.60% (WR 47.0%) | +237.6% | -41.8%/-17.6% |
| 🏆 **Momentum (Scenario 3)** | **55** | **5.0%** | **10.0%** | **+100.57% (WR 43.1%)** | **+232.78% (WR 43.6%)** | **+567.8%** | -22.0%/-25.4% |

**תובנת המפתח:** תרחיש 3 מציג יציבות Win Rate פנומנלית (43.1% מול 43.6%) בשני משטרי שוק שונים לחלוטין - מעיד על חוסן סטטיסטי גבוה והיעדר Overfitting.

> ⚠️ **הסתייגות חשובה לגבי כל המספרים בטבלה זו:** הם חושבו לפי מודל כניסה של **Market Order במחיר הפתיחה** (`backtester.py:223`). מאז אוגוסט 2026 הייצור עובד ב**הוראת לימיט קשיחה**, ולכן הטבלה אינה מתארת את התנהגות המערכת בפועל. ראה **סעיף 3.1** לפירוט מלא של הפער ותוכנית הטיפול.

---

## 3. כללי מסחר וזרימת עבודה שבועית (Trading Rules)

### לוח זמנים שבועי (שעון ישראל)

**יום ראשון בערב (21:00-22:00):** הרצת הסורק השבועי
1. גישה ל-`/run-scan`, בדיקת פרמטרים (ברירת מחדל: RSI=55, SL=5%, TP=10%)
2. הוספת הפקדה במידת הצורך
3. הרצת הסריקה (~2-3 דקות) - מנתחת ~100 מניות, מזהה עד 3 הזדמנויות טובות ביותר, וממלאת סלוטים פנויים בלבד (לא סוגרת פוזיציות קיימות)

**יום שני בבוקר (16:30 שעון ישראל = 9:30 בבוקר שעון ניו יורק):** ביצוע בפועל
1. סקירת ההוראות הממתינות בטבלת "Pending Entries" בדשבורד
2. הזנת **Limit Orders מסוג GTC** במחיר היעד (= מחיר הסגירה של יום שישי) עבור כל הוראה
3. הגדרת Stop Loss ב-Entry × 0.95, Take Profit ב-Entry × 1.10 - **לפי מחיר המילוי בפועל**
4. המערכת תזהה אוטומטית (רבע שעה אחרי הפתיחה ואילך, ובכל רענון) אילו הוראות בוצעו
5. **הוראות שלא בוצעו נשארות פעילות** - אין צורך לעשות דבר. אם החלטת לוותר על סיגנל, לחץ "✕ בטל הוראה" בדשבורד **וגם** בטל אצל הברוקר

### כללי כניסה - מודל הוראת לימיט (אוגוסט 2026) 🆕

**הבעיה שנפתרה:** עד אוגוסט 2026, סיגנל שנמצא בסריקת יום ראשון נרשם מיידית כפוזיציה `ACTIVE` במחיר הסגירה של יום שישי. בפועל, ההוראה אצל הברוקר היא **הוראת לימיט** שמתבצעת רק אם המחיר באמת נגע במחיר היעד ביום המסחר הבא. מניה שנפתחה בגאפ מעל היעד ולא חזרה אליו - **לא נקנתה**, אבל המערכת דיווחה עליה כפוזיציה רווחית (רווח פנטום), תפסה סלוט, ושיריינה מזומן.

**דוגמה מתועדת:** CRWD, סריקת 2026-08-09, יעד $214.42. ביום המסחר 2026-08-10 המניה נפתחה ב-$215.31 והנמוך שלה היה **$214.55** - כלומר מעולם לא נגעה ביעד. המערכת דיווחה על +5.25% שלא היה קיים.

**מכונת המצבים (GTC - Good-Til-Cancelled):**
```
סריקה → PENDING_ENTRY ──(המחיר נגע ביעד)────→ ACTIVE → HIT_TP / HIT_SL
                       └─(ביטול ידני בלבד)──→ NOT_FILLED
```

**כללי המילוי (לימיט קשיח GTC), ב-`evaluate_limit_fill()`:**
1. אם הפתיחה של סשן כלשהו ≤ מחיר היעד → מילוי **במחיר הפתיחה** (הוראה marketable מתבצעת מיד, במחיר הטוב יותר).
2. אחרת, אם ה-Low של נר כלשהו נגע ביעד → מילוי **במחיר היעד**.
3. אחרת → נשאר `PENDING_ENTRY` **ללא הגבלת זמן**.

**🔑 ההוראה לעולם לא פוקעת מעצמה.** זו הגדרת GTC: ההוראה נשארת חיה על פני ימים ושבועות, ונסרקת מחדש בכל בדיקה על **כל ימי המסחר** שחלפו מאז הונחה (לא רק היום הראשון). המטרה: לתת למניה יום-יומיים לתקן אחרי מימוש רווחים, ועדיין לתפוס אותה. **הדרך היחידה לסגור הוראה היא כפתור "✕ בטל הוראה" בדשבורד.**

**נקודות מפתח:**
* **`ENTRY_CHECK_DELAY_MINUTES = 15`** - הבדיקה הראשונה מתבצעת רק רבע שעה אחרי הפתיחה, כדי לתת ל-yfinance לפרסם נרות.
* **`INTRADAY_HISTORY_LIMIT_DAYS = 55`** - yfinance מספק נרות 5 דקות רק ל-~60 יום אחורה. הוראה ותיקה מכך תישאר `PENDING` עם הודעה מתאימה, במקום להיפתר בטעות. במקרה כזה יש לסנכרן ידנית ("מולא ידנית" / "בטל הוראה").
* **סינון שעות מסחר** - נלקחים רק נרות בין 09:30 ל-16:00 (RTH). מסחר מוקדם/מאוחר לא מפעיל הוראת לימיט רגילה.
* **`sessions_waiting`** - נשמר על כל הוראה ומוצג בדשבורד. מ-3 ימי מסחר הערך נצבע כתום, מ-5 אדום עם ⚠️ - תזכורת ויזואלית שהסיגנל מתיישן וכדאי לשקול ביטול.
* **חישוב מחדש של SL/TP** - במילוי, `stop_loss` ו-`take_profit` מחושבים מחדש לפי **מחיר המילוי בפועל** (לכן נשמרים `stop_loss_pct`/`take_profit_pct` על כל טרייד), וכך גם `quantity = reserved_capital / fill_price`.
* **שריון הון** - `PENDING_ENTRY` תופס סלוט ומשריין מזומן (`reserved_capital`), כדי שסריקה נוספת לא תקצה את אותו כסף פעמיים. `NOT_FILLED` משחרר הכל ולא גובה עמלה.
* **הרצה אוטומטית** - `check_pending_entries()` נקראת בראש `update_portfolio_status()`, ולכן רצה בכל `/refresh-tracker` (כל 15 דקות מה-UI) ובסוף כל סריקה.
* **תזמון הסשן** - `get_entry_session()` מוצאת את **סשן ה-NYSE הראשון שנפתח אחרי הסיגנל** (`pandas_market_calendars`), כך שחגים, סופי שבוע וימי מסחר מקוצרים מטופלים נכון.

**פרדיקטים מרכזיים (`trading_logic.py`) - מקור אמת יחיד לסטטוסים:**
| פונקציה | מחזירה True עבור | משמשת ל- |
|---|---|---|
| `is_open_position()` | `ACTIVE` בלבד | P&L לא ממומש, הון מושקע |
| `holds_slot()` | `ACTIVE` + `PENDING_ENTRY` | ספירת סלוטים ושריון מזומן |
| `is_closed_trade()` | `HIT_TP`/`HIT_SL`/`MANUAL_CLOSE` | Win Rate, P&L ממומש, סימולציה, MWR |
| `is_not_filled()` | `NOT_FILLED` | תצוגת "סיגנלים שהוחמצו" |

**קריטי:** `PENDING_ENTRY` ו-`NOT_FILLED` מוחרגים מכל חישוב סטטיסטי. לפני התיקון, פילטרים כמו `status != 'ACTIVE'` היו סופרים אותם כעסקאות סגורות עם P&L=0 ומעוותים את ה-Win Rate.

**ממשק משתמש:** טבלת **"⏳ Pending Entries"** בדשבורד מציגה מחיר יעד, מחיר נוכחי, מרחק באחוזים, תאריך תחילת ההוראה, מספר ימי המסחר שההוראה ממתינה, וההון המשוריין. לכל שורה שני כפתורים:
* **"מולא ידנית"** - לסנכרון כשהברוקר ביצע במחיר שונה ממה ש-yfinance מראה.
* **"✕ בטל הוראה"** - הדרך היחידה לסגור הוראת GTC. מציג אישור עם פירוט ההשלכות (שחרור סלוט והון, ללא עמלה) ותזכורת לבטל גם אצל הברוקר.

בנוסף כפתור **"בדוק עכשיו"** להרצת בדיקה מיידית. Endpoints: `POST /api/check-pending`, `POST /api/fill-pending`, `POST /api/cancel-pending`.

**כלי ביקורת רטרואקטיבי:** `audit_entries.py` בודק פוזיציות `ACTIVE` קיימות מול הנתונים ההיסטוריים ומזהה "פוזיציות פנטום". תחת GTC הוא **מחזיר אותן ל-`PENDING_ENTRY`** (ולא ל-`NOT_FILLED`) - כי ההוראה עדיין חיה ועשויה להתמלא; ההון עובר מ-`invested_capital` ל-`reserved_capital` וה-P&L הפנטומי נמחק. ברירת המחדל dry-run; `--apply` מבצע אחרי גיבוי אוטומטי. הכלי שמרני: תוצאה לא חד-משמעית (`UNKNOWN`) לא נוגעת בטרייד.

---

## 3.1 ⚠️ פער ידוע: מודל הכניסה בבקטסט מול הייצור (Open-vs-Limit Gap)

**סטטוס: פתוח, לא טופל. מתועד לטיפול עתידי.**

### מהות הפער

שתי מערכות בפרויקט מדמות את אותה אסטרטגיה, אבל **נכנסות לעסקה בצורה שונה לגמרי**:

| | קובץ ושורה | מודל הכניסה | האם תמיד נכנס? |
|---|---|---|---|
| **בקטסט** | `backtester.py:223` — `entry_price = float(t_df.loc[current_day, 'Open'])` | **Market Order** במחיר הפתיחה של יום שני | ✅ תמיד |
| **ייצור** | `trading_logic.evaluate_limit_fill()` | **Limit Order** קשיח במחיר הסגירה של שישי | ❌ רק אם המחיר נגע ביעד |

### למה זה קריטי

מטריצת החוסן בסעיף 2 (**+567.8%**, Win Rate ~43%) חושבה **כולה** לפי מודל ה-Market Order. מאז אוגוסט 2026 הייצור עובד בלימיט קשיח (סעיף "כללי כניסה"), ולכן:

> **המספרים במטריצת החוסן אינם מתארים את מה שהמערכת עושה כיום בפועל.**

זו אינה סטייה תיאורטית. באסטרטגיית מומנטום, סיגנל שנפתח בגאפ מעל מחיר היעד הוא לרוב סיגנל **חזק** — בדיוק אלה שהלימיט הקשיח מפספס באופן שיטתי. כלומר הלימיט מבצע סינון שאינו אקראי אלא **מוטה נגד העסקאות הטובות**.

### דוגמה מוחשית מהתיק

CRWD, סריקת 2026-08-09, יעד $214.42:
* **בבקטסט:** היה נכנס ב-$215.31 (מחיר הפתיחה) — עסקה קיימת.
* **בייצור:** הנמוך של היום היה $214.55, מעולם לא נגע ביעד — **אין עסקה**.

אותו סיגנל בדיוק, שתי תוצאות הפוכות.

### מה צריך לעשות (כשנחזור לזה)

1. **להוסיף פרמטר `entry_mode` ל-`backtester.py`:**
   * `'open'` — התנהגות נוכחית (Market at Open), לשימור השוואתיות להיסטוריה.
   * `'limit_prev_close'` — מודל הייצור: כניסה רק אם `Low ≤ signal_price`; אם הפתיחה מתחת ליעד — כניסה בפתיחה (המחיר הטוב יותר); אחרת הסיגנל נזרק.
2. **להריץ מחדש את שתי התקופות** (Chaos 2021-2026, Calm 2010-2020) בשני המצבים.
3. **לתעד את שתי העמודות זו לצד זו** במטריצת החוסן, כדי שיהיה ברור איזה מספר שייך לאיזה מודל.
4. **להחליט על בסיס הנתונים** — שלוש האפשרויות שנשקלו:
   * **A. לימיט קשיח** (המצב הנוכחי) — נאמן ל-100% למה שקורה בפועל בחשבון.
   * **B. Market at Open** — מיישר את הייצור לבקטסט; דורש שינוי הרגל: הזנת Market Order בפתיחה במקום לימיט.
   * **C. היברידי** — לימיט עם סבילות (למשל עד +0.5% מעל היעד), שמסנן רק גאפים קיצוניים.

### נקודות שחייבות תשומת לב בבדיקה

* **מדד ההשוואה הנכון הוא תוחלת לכל *סיגנל*, לא לכל *עסקה*.** מודל הלימיט מייצר פחות עסקאות; אם משווים רק ממוצע לעסקה, מתעלמים מהעלות של הסיגנלים שלא נלקחו כלל.
* **השפעה על תפוסת הסלוטים:** פחות מילויים = יותר זמן עם מזומן לא מנוצל. יש למדוד גם את שיעור הניצול של ההון, לא רק את התשואה לעסקה.
* **יש לבדוק את שני משטרי השוק בנפרד.** ממצא שמופיע רק ב-Chaos או רק ב-Calm אינו מסקנה יציבה.
* **`backtester.py` נכנס רק ביום הראשון של השבוע** (`day_idx == 0`), בעוד שבייצור ההוראה חיה עד סגירת אותו יום מסחר בלבד — יש לוודא שההשוואה מיישרת גם את חלון הזמן הזה.

### קבצים שיושפעו

`backtester.py` (פרמטר ולוגיקת הכניסה), `app.py` (`/api/backtest` — העברת הפרמטר), `ui_generator.py` (הצגת המצב במטריצה), וסעיף 2 במסמך זה (עדכון הטבלה).

### כללי יציאה (קריטי!)

פוזיציות נסגרות **רק** כאשר Stop Loss או Take Profit נפגעים. **אין** סגירה לפי זמן, החלטה ידנית, או שינוי תנאי שוק - "תן לרווחים לרוץ, חתוך הפסדים מהר".

**חריגה מתועדת - סגירה ידנית (Manual Close):** לצורך מקרי קצה בלבד (תיקון טעויות רישום, סנכרון עם הברוקר בפועל, מצבים חריגים שהמערכת האוטומטית לא מכסה), קיים כפתור "Close" בטבלת ה-Active Positions בדשבורד (`ui_generator.py`) שפותח modal לאישור סגירה ידנית. ה-modal כולל שדה `exit_price` (ברירת מחדל: המחיר הנוכחי, ניתן לעריכה) ושדה `exit_timestamp` (תאריך/שעה בדיעבד, ברירת מחדל: עכשיו, ניתן לעריכה חופשית לצורך תיעוד סגירות שקרו בעבר). הפעולה שולחת בקשת POST ל-`/api/close-trade` (ב-`app.py`), אשר קוראת לפונקציה `close_trade_manually()` ב-`trading_logic.py` ולאחריה ל-`data_manager.update_portfolio_state()` כדי לסנכרן את מצב הפורטפוליו והאנליטיקס. **חשוב:** זו חריגה מודעת ומתועדת לכלל "SL/TP בלבד" - אין להשתמש בה כחלק מהאסטרטגיה השוטפת, אלא רק לתחזוקה/תיקון נתונים.

**עמודת "Value" (שווי פוזיציה בדולרים):** בטבלת Active Positions קיימת עמודה בין Price ל-P&L המציגה את שווי השוק הנוכחי (`quantity × current_price`) בשורה הראשית, ומתחתיה את עלות הכניסה (`quantity × entry_price`) באפור. הצבע (ירוק/אדום) נקבע לפי היחס בין השניים, בעקביות עם עמודת ה-P&L. עבור רשומות ישנות ללא שדה `quantity`, הכמות נגזרת מ-`batch_position_size / entry_price`.

### נוסחת גודל פוזיציה

```
Position Size = Available Cash / divisor
Available Cash = Total Deposits + Realized P&L - Invested Capital
divisor = min(Empty Slots, Setups Found), עם רצפה של 2 (אלא אם Setups Found ≥ Empty Slots)
```

**עדכון (יולי 2026):** גודל הפוזיציה מחושב **לאחר** שידוע כמה הזדמנויות נמצאו בפועל בסריקה, ולא רק לפי מספר הסלוטים הפנויים. כך המזומן לא "יושב" בחוסר-שימוש כאשר נמצאות פחות הזדמנויות מהסלוטים הפנויים:

* תיק ריק (3 סלוטים) + נמצאה **1** הזדמנות → **50%** מהמזומן (ולא 33%)
* תיק ריק (3 סלוטים) + נמצאו **2** הזדמנויות → **50%/50%**
* תיק ריק (3 סלוטים) + נמצאו **3** הזדמנויות → **33%/33%/33%** (כל הסלוטים מתמלאים, אין רצפה)
* פוזיציה אחת פעילה (2 סלוטים) + נמצאה **1** הזדמנות → **50%** מהמזומן הפנוי

הרצפה של "לא לחלק ביותר מ-2" (כלומר, לעולם לא לשים 100% מהמזומן על מניה אחת) נשמרת כדי להימנע מריכוזיות יתר וסיכון SL חד-פעמי גבוה מדי, ותואמת את פרמטרי הבקטסט (3 סלוטים). ראה `calculate_position_size()` ב-`analytics_generator.py` ו-`add_new_trades()` ב-`trading_logic.py`.


### הגבלות תפעוליות

* **פילטר שוק:** הסורק פועל רק כאשר SPY מעל SMA 200. בשוק דובי - אין כניסות חדשות (פוזיציות קיימות ממשיכות לפעול עם SL/TP רגילים).
* **פילטר דוחות:** מניות עם דוחות כספיים בשבוע הקרוב מוחרגות אוטומטית.
* **עמלות:** $2.5 לעסקה (כניסה+יציאה), בנוסף עמלת המרה בהפקדות ILS→USD - ראה סעיף 4.1 למטה (עמלה גמישה, ניתנת לעריכה, ברירת מחדל $3.0).

### היסטוריית תיקוני גודל פוזיציה

* **יוני 2026:** לוגיקת גודל הפוזיציה תוקנה כך שהיא מחלקת את המזומן הזמין במספר הסלוטים *הפנויים בפועל* (ולא תמיד ב-3 הקבוע).
* **יולי 2026:** שיפור נוסף - החלוקה מתחשבת גם במספר ההזדמנויות שנמצאו בפועל בסריקה (ולא רק בסלוטים הפנויים), כדי שמזומן לא יישאר חסר-שימוש כשנמצאות פחות הזדמנויות מסלוטים פנויים. ראה נוסחה מעודכנת למעלה.
* **אוגוסט 2026:** תוקן באג תצוגה בדשבורד - "Next Position" חושב תמיד לפי 3 סלוטים (`active_positions` לא הועבר ל-`calculate_position_size`), בעוד שבפועל הסריקה כבר חילקה נכון לפי הסלוטים הפנויים. כעת התצוגה מחשבת `Cash Available / (3 - active positions)` ומציגה גם כמה סלוטים פנויים.



---

## 4. Trade Analytics & Capital Management

### מעקב מצב פורטפוליו
* **Current Equity** = Deposits + Realized P&L + Unrealized P&L
* **Cash Available** = Deposits + Realized P&L - Invested Capital
* **Position Sizing** = Cash Available / Max Positions (3)

### דשבורד `/trade-analytics`

מציג מדדי ביצוע מרכזיים: Total Trades, Win Rate, Avg Duration, Profit Factor, **MWR Return**, **Strategy Simulation**, SPY Return (לתקופת הסימולציה), Alpha, וכן פירוט הסימולציה (הון סופי מדומה, כמה סיגנלים נלקחו/הוחמצו). גרפי Plotly.js (MWR מול SPY, Duration vs P&L, התפלגות רווח/הפסד), ופילטרים מתקדמים (סטטוס, ביצועים, טווח תאריכים).

**אוגוסט 2026 - תיקון מהותי (Simple Return / TWR הוסרו):** התגלה שהמדדים "Simple Return" ו-"TWR" (שנוסה בקצרה) היו מוטים משמעותית ע"י:
1. תזמון הפקדות לא אחיד (הון קטן בהתחלה → אחוזים מנופחים).
2. גודל פוזיציה לא-עקבי בין עסקאות (בין 1.1% ל-35% מהקופה בפועל), מה שגרם למדדים משוקללי-הון לתת משקל לא הוגן לעסקאות "מזל התזמון".
3. עסקאות כפולות שנרשמו פעם לכל owner (matan/horim) עבור **אותו סיגנל** בפועל (SNOW, ROKU) - הוכפלו בטעות בחישובים ראשוניים.
4. Alpha חושב מול טווח תאריכים לא תואם ל-SPY, מה שניפח את המספר (53%+) ללא הצדקה.

**הפתרון:** נבנתה `simulate_strategy_growth()` - סימולציה אמיתית (לא נגזרת סטטיסטית) שעונה במדויק על השאלה "מה היה קורה אם הייתי נכנס מההתחלה עם $X ועוקב אחרי השיטה בדיוק, כולל חוק חלוקת המזומן ל-3 סלוטים". היא כוללת דה-דופליקציה של סיגנלים (`_dedupe_signals()`, לפי ticker+entry_price+exit_price - אג-נוסטי לטיימסטמפ כי כל owner מבצע בזמן קצת שונה), ומחשבת SPY benchmark על טווח התאריכים המדויק של הסימולציה עצמה (לא של MWR) כדי שה-Alpha יהיה הוגן.

**תוצאה מאומתת (אוגוסט 2026):** $10,000 → $12,460.10 (**+24.6%**) על פני ~2.5 חודשים, מתוך 19 סיגנלים ייחודיים (17 נלקחו, 2 הוחמצו עקב חוסר סלוט פנוי), מול SPY שעשה כ-4.7% באותה תקופה (**Alpha = +19.91%**). MWR (הכסף האמיתי, עם הפקדות מדורגות) הראה +6.28% - נמוך יותר כי הכסף לא היה מנוצל תמיד ב-100% (המתין להפקדות עתידיות).

נתוני MWR מקובצים לפי תאריך יציאה (נקודה אחת ליום) כדי למנוע עומס ויזואלי ולחשב תשואה מצטברת נכונה על פני כל ההיסטוריה.

**סדרת תשואה יומית לסימולציה בגרף:** `calculate_simulation_cumulative_return()` (`analytics_generator.py`) בונה סדרת תשואה מצטברת (%) יום-אחר-יום מתוך `trades_log` שמייצרת `simulate_strategy_growth()`, כדי שקו הסימולציה יוצג בגרף ה-Cumulative Returns לצד MWR ו-SPY (משתנה `simulation_returns` ב-`prepare_analytics_data()`, מוצג ב-`/trade-analytics` תחת "Strategy Simulation ($10K)").

### 4.1 ניהול הפקדות ושערי המרה (FX Management) - אוגוסט 2026

הוסרה העמלה הקבועה הישנה ($10.27) לטובת מערכת גמישה וניתנת לעריכה מלאה, המנוהלת ב-`currency_manager.py` ומאוחסנת ב-`deposits_history.json` (`metadata.default_fx_rate`, `metadata.default_deposit_fee_usd`):

* **עמלת המרה ברירת מחדל (`default_deposit_fee_usd`)** - מתחילה ב-$3.0, ניתנת לעריכה גלובלית (`update_default_deposit_fee()`), וניתן גם לדרוס אותה לכל הפקדה ILS בנפרד (שדה בטופס ובמודל העריכה).
* **שער ברירת מחדל (`default_fx_rate`)** - משמש כ"רשת ביטחון" עבור הפקדות ישנות/חסרות שער תקין בחישוב הממוצע המשוקלל (`get_weighted_avg_fx_rate()`). מחושב אוטומטית בפעם הראשונה (ממוצע משוקלל של כל ההפקדות עם שער תקין), ולאחר מכן ניתן לעריכה ידנית (`update_default_fx_rate()`).
* **שער היסטורי לפי תאריך (`get_historical_usd_ils_rate()`)** - כאשר מזינים תאריך הפקדה בעבר (בטופס `/run-scan` או בעריכת הפקדה), השער נשלף מ-Yahoo Finance ליום המסחר הקרוב ביותר לאותו תאריך (ולא שער "היום"), כדי לשקף במדויק את השער שהיה בפועל ביום ההפקדה. יש fallback לשער החי אם אין נתונים היסטוריים.
* **ניהול הפקדות (CRUD)** - מודל "ניהול הפקדות ושערי המרה" בדשבורד הראשי (`ui_generator.py`, כפתור בכרטיס "TOTAL P&L IN ILS") מציג טבלה של כל ההפקדות עם אפשרות לערוך (תאריך, סכום, שער, עמלה) או למחוק כל רשומה. כל שינוי מפעיל `_recalculate_metadata_from_deposits()` שבונה מחדש את כל הסכומים המצטברים (`total_deposits_usd_net`, `avg_conversion_fee` וכו') מ-`deposits` כ-single source of truth, ומרענן את הדשבורד.
* **הפקדה ידנית (ללא סריקה)** - כפתור "➕ הוסף הפקדה ידנית" בראש מודל ניהול ההפקדות פותח מודל `addDepositModal` (תאריך, מטבע ILS/USD, סכום, שער המרה אופציונלי, עמלת המרה) עם תצוגה מקדימה חיה של הנטו לתיק. השליחה מגיעה ל-`POST /api/deposits` שקורא לאותו `currency_manager.add_deposit()` של זרימת הסריקה, ולכן פורמט הרשומה זהה לחלוטין. התיאור נוצר אוטומטית: `Manual deposit - <date>`. שדה השער ריק = שליפה אוטומטית של השער ההיסטורי לפי התאריך.
* **`_sync_deposits_metadata()` (`app.py`)** - פונקציית עזר משותפת ל-`execute_scan()` ול-`api_create_deposit()` שמשקפת את הסכומים המצטברים מ-`deposits_history.json` אל `portfolio_metadata` (DRY). בזכות שרשרת התלויות `total_deposits → calculate_portfolio_state() → cash_available → next_position_label`, כרטיס "CASH AVAILABLE" ושורת "Next Position" מתעדכנים מיידית אחרי הפקדה (אומת בבדיקת קצה-לקצה: הפקדה של $500 העלתה את ה-cash בדיוק ב-$500).
* **Endpoints רלוונטיים (`app.py`):** `GET /api/deposits`, `POST /api/deposits` (הפקדה ידנית חדשה, עם ולידציה: סכום > 0, מטבע ILS/USD, תאריך YYYY-MM-DD), `POST /api/deposits/<id>`, `DELETE /api/deposits/<id>`, `POST /api/default-fx-rate`, `POST /api/default-deposit-fee`.
* **טופס `/run-scan`** עודכן לתמיכה בבחירת תאריך ההפקדה, סוג מטבע (ILS/USD), ועמלת המרה (מולאת מראש מברירת המחדל, ניתנת לעריכה) לפני ביצוע הסריקה השבועית.

---

## 5. Server-Sent Events (SSE)

כדי למנוע קפיאת ה-UI במהלך סריקת ~100 טיקרים, פס התקדמות ולוג פעילות חי פועלים באמצעות SSE (`/api/scan-stream`, `/api/dry-run-stream`).

---

## 6. Smart Diagnostics for Zero-Setup Scans

כאשר סריקה מחזירה 0 הזדמנויות, המערכת מציגה אבחון חכם: אחוז מניות שנותחו, כמה חצו RSI, כמה במיקום מחיר תקין, כמה נפסלו עקב דוחות קרובים - עם המלצה מעשית (לחכות, לשנות פרמטרים, וכו').

---

## 7. Roadmap

* 🔴 **[עדיפות גבוהה] יישור מודל הכניסה בין הבקטסט לייצור** - הוספת `entry_mode` ל-`backtester.py` והרצה מחדש של מטריצת החוסן. ראה **סעיף 3.1** לפירוט מלא. עד שזה ייסגר, אין להסתמך על מספרי מטריצת החוסן כתחזית לביצועי המערכת הנוכחית.
* Multi-threading / Async Scanning לסריקה מהירה יותר.
* Trailing Stop Loss למנוע המומנטום.
* Webhook Notifications (Discord/Telegram) עבור הזדמנויות חדשות.
* Enhanced Analytics: Drawdown analysis, Sharpe ratio, Trade correlation.
